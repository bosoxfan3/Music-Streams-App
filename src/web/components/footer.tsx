const Footer = () => (
    <div className="-mx-4 mt-2 px-4 py-3 text-right text-gray-700 text-sm bg-gray-100"> {/* set negative margin on footer to extend it the full width */} 
        <p>Spotify Streams</p> {/* hardcoded text because I only have 'spotify streams' as a comparison option */} 
    </div>
);

export default Footer;
